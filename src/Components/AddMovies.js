import React,{useState, useContext} from 'react'
import {moviesConsumer} from './ContextMovies';

export default function AddMovies() {
   // const value=useContext(moviesConsumer);
    return (
        <div>
           Add Movies 
        </div>
    )
}
